spring:
  application:
    name: quality-lab
  datasource:
    url: jdbc:h2:mem:qualitylab
    username: test
    password:
    driverClassName: org.h2.Driver

  jpa:
    hibernate.ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect